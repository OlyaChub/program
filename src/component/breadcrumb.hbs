<div class="col-12">
    <div class="row-8">
        <div class="col-12">
            <div class="breadcrumb">
                {{#each breadcrumb}}
                <div class="breadcrumb-item">
                    <a href="{{url}}">{{name}}</a>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</div>