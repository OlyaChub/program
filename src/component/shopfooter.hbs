<div class="row mb-5 g-4">
    {{#each footer}}
    <div class="col-2 p-3">
        <ul class="nav">
            {{#each this}}
            <li>
                <a class="nav-link {{#if @last}}disabled{{/if}}" href="{{link}}">{{text}}</a>
            </li>
            {{/each}}
        </ul>
    </div>
    {{/each}}
</div>