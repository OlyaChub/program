{{#each footer}}
<div class="row">
    {{#each this}}
    <div class="col-3">
        <div class="text-primary" class="nav-item" class="p-3">
            <a href="{{link}}" class="nav-link">{{text}}</a>
        </div>
    </div>
    {{/each}}
</div>
{{/each}}